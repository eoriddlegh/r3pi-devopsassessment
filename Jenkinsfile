node {
    stage('Example') {
//        try {
            echo 'print out docker version'
            //sh 'docker --version'
            sh 'exit 0'
/*        }
        catch (exc) {
            echo 'Something failed, I should sound the klaxons!'
            throw
        }
*/    }
}
